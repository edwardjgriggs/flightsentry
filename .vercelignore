# Local dependency and build output
.git/
.vercel/
.next/
node_modules/
.venv/

# Raw ESA archives, extracted working data, and staged models.
# Patterns are anchored to the repo root so public/data/ still deploys.
/data/
/artifacts/

# Local validation output
/playwright-report/
/test-results/
/coverage/
/reports/
/.playwright-mcp/

# Unused WebGPU execution-provider binaries (the app loads the wasm EP only)
public/ort/*.jsep.*

# Local development and environment state
/.bob/
/.c*/
.env*
*.tsbuildinfo
__pycache__/
*.py[cod]
